# Session Memory — 2026-07-02

## Major Changes Applied

### AGENTS.md — CONVERSATION ROUTING FLOWS added
- Six flows: Hot Lead / Escalation (1), Direct Order (2), Client Not Responding (3), Human-Owned Cold (4), Store Location (5), Complaint Handling (6)
- Action classification table: ask-first vs just-do-it split with reasoning
- Old Section 6 (Human Handoff) removed, merged into FIRST FLOW
- NO DOUBLE-MESSAGING RULE amended: carve-out for scheduled follow-up cadences in Flows 3/4/5
- Global Rules: 5 rules applied across all flows

### db.py — Major Refactor
- **`set_category()` is the SOLE writer of the `category` column** — all other write paths eliminated
- `upsert_customer()` no longer touches category at all (removed from INSERT/UPDATE SQL)
- Per-phone lock added via atomic `os.makedirs` mutex (timeout 5s) to prevent concurrent write races
- Dedup: skips write if old_cat == cat (already at target)
- Auto-saves `previous_owner` when moving FROM human-owner category TO followup/junk/complaints/hot leads
- Categories extended: `followup`, `junk`, `complaints`, `ahsan`, `ahmed`, `imran`, `rafay` added
- `cadence_status` column added to customers table (legacy from Option B, now unused but kept)
- Logging: every set_category prints old→new transition + previous_owner in JSON

### Option A — Tag Switching (not Option B)
- When human-owned chat (ahsan/ahmed/imran/rafay) goes cold: category switches to followup/junk
- Owner name auto-saved to `previous_owner` column — never lost
- One tag per chat at all times — no multi-tag rows

### TOOLS.md — Fully rewritten
- Two-field design documented: category + cadence_status
- Valid categories table, cadence statuses table
- Commands documented: set-category, set-cadence-status, list-customers
- Tagging-by-flow matrix
- TODO: swap for real WhatsApp Business API labels

### MEMORY.md — Follow-Up State Tracking section added
- Template format for tracking: flow, followup_week, last_followup_date, list_tag, human_owner

### Customer Categories skill — Updated
- Now calls BOTH `upsert-customer` AND `set-category "new customer"` for first-time contacts (since upsert no longer writes category)

## Owner
- Hadi / +923362615506 — verified owner

## Gateway
- Restarted at 2026-07-02 11:09:11 PKT after db.py edits
- `systemctl --user restart openclaw-gateway.service`

## DB State After Tests
- +923362615506: category="complaints", previous_owner="ahsan"
- All 6 flows tested; every write produced exactly one category value
